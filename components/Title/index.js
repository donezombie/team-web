import React from "react";

const propTypes = {};

const Title = ({ title = "" }) => {
  //! State

  //! Function

  //! Render
  return (
    <div className="title-common wow fadeIn">
      <h2>{title}</h2>
    </div>
  );
};

Title.propTypes = propTypes;
export default Title;
